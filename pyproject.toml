[project]
name = "my-mlflow"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "dspy>=3.0.4",
    "mlflow>=3.6.0",
    "psycopg2-binary",
    "python-dotenv",
]
